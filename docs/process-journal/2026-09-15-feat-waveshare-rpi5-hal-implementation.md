2026-09-15 · gate · pnpm check · The repo's two mandatory pre-PR gates could not run on the machine: no node, npm, npx or node_modules, and no node.exe under Program Files, LOCALAPPDATA or APPDATA. The style gate's rules were re-run from a port of scripts/check-docs.mjs; the build's dead-link check has no substitute and routes to CI. (self)
2026-09-15 · brief · service provider artifact · The brief asked the I/O section to name the Mender artifact that carries the service provider; artifacts-mender at origin/main carries no such artifact and STYLE.md § Things That Do Not Exist Yet forbids naming it, so the section says a Mender update without the name, and the name changed upstream the same day. (self) → codified: docs/STYLE.md
2026-09-15 · brief · service-provider-protocol · The brief gave "a get carries no body, no content type, no schema" as a general hw/* rule; it holds for the di and do get legs but not for hw/modbus/get, which carries GetModbusPayload, so it was not written as a rule. (self)
2026-09-15 · brief · branch name · The brief pinned branch `docs/ipcbox-cm5-and-json-hw-wire` and its acceptance criterion; /vion-git:branch § 2 allows only feat, fix, chore and explore as the kind. The name was kept and the break surfaced to the human instead. (self)
2026-09-15 · review · claim scopes · The branch review found three claims at the wrong scope: the /root SSH-key loss pinned to the first OS update rather than the one introducing the overlay, the hw/* schema-label rule written for all built-in contracts when the get legs carry no label, and the boot time narrowed to the first boot. STYLE.md § Accuracy should have prevented it. (self)
2026-09-15 · review · service-provider-protocol · The branch review found application/x-flatbuffers dropped from the operational-phase Content-Type row and the custom-format table; it is live SDK surface and outside the brief's hw/* scope. The brief's "Remote/Func stay FlatBuffers" should have prevented it. (self)
2026-09-15 · review · services, onboarding · The branch review found the GPIO-inversion mechanism and the boot-partition mechanism written onto pages whose readers can observe neither. STYLE.md § Economy "Cut mechanism and degraded cases" should have prevented it. (self)
2026-09-15 · review · services, onboarding · The branch review found a binding instruction and a full flashing procedure placed ahead of the sentence saying neither can be run yet. STYLE.md § Economy "State optionality first" should have prevented it. (self)
2026-09-15 · review · onboarding, troubleshooting · The branch review found the board's boot duration and its missing activity LED stated in full on both pages. STYLE.md § When to Add a Page "Never explain the same thing twice" should have prevented it. (self)
2026-09-15 · review · process-journal · The branch review found the unshipped artifact identifier printed twice in the journal line recording the decision not to print it, in a public repo. The rule codified on this same branch, STYLE.md § Things That Do Not Exist Yet, should have prevented it. (self)
2026-09-15 · brief · service-provider-protocol · The brief specified the protocol page's request shape as body, content type and schema; MQTT v5 correlation data is required too and the page never said so, which a bench run found when the provider SDK dropped requests without it. Amendment 1 supplied it. (self)
2026-09-15 · brief · service-provider-protocol · Amendment 1 asked for a correlation clause per request-topic table row; the rule is identical for every row, so it is stated once in the message-conventions table and once at the request-response pattern. STYLE.md § Economy "Never restate a table in prose". (self)
2026-09-15 · review · service-provider-protocol · The amendment review found the correlation rule wrong in both directions at once: "dropped before any handler sees it" is false for everything the Dale runtime consumes, and scoping it to the operational phase excluded the registration request, which Mesh guards the same way. STYLE.md § Accuracy should have prevented it. (self)
2026-09-15 · review · service-provider-protocol · The amendment review found the requester's obligation to set correlation data deleted from the request-response definition, leaving only the responder's echo — weaker on the amendment's own point than before the commit. STYLE.md § Accuracy "a confident wrong sentence is worse than a missing one". (self)
2026-09-15 · review · service-provider-protocol · The amendment review found the Last Will table still omitting will correlation data; a will is CONNECT options, not a published message, so the conventions row never reached it and a provider built from the table is never reported offline. (self)
