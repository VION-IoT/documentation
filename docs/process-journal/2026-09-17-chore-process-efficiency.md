2026-09-17 · decision · pre-PR obligations · The spec allowed naming CI as the gate instead of listing obligations; CLAUDE.md § Commands and § Conventions both told a contributor to run `pnpm check` before a pull request, so the obligations kept `pnpm check` and `pnpm build` locally under triggers on `docs/**`, and those two restatements were deleted instead.
2026-09-17 · decision · CLAUDE.md budget · CLAUDE.md came to 10,228 bytes against the 10,240-byte budget the same change names — twelve bytes of headroom, reached only by deleting four restatements.
