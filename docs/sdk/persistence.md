---
title: Persistence
description: Using the Persistent attribute to persist logic block state across restarts, including what gets persisted and known limitations.
---

# Persistence

## The `[Persistent]` Attribute

How to mark fields and properties for automatic persistence using `[Persistent]`.

## What Gets Persisted

Which types of data can be persisted and how the persistence store works.

## State Recovery After Restart

How persisted state is restored when a logic block starts up, including ordering guarantees.

## Limitations

Known limitations of the persistence system (size limits, unsupported types, timing considerations).
