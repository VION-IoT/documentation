---
title: Properties & Measuring Points
description: Defining service properties and measuring points using attributes, including types, categories, display hints, and persistence behavior.
---

# Properties & Measuring Points

## The `[ServiceProperty]` Attribute

How to declare a property on a logic block using `[ServiceProperty]` and what parameters it accepts.

## Property Types

Supported data types for properties (bool, int, double, string, enums, etc.).

## Getters and Setters

How to implement read-only, write-only, and read-write properties.

## Categories

Organizing properties into logical categories for dashboard display.

## Display Hints

Controlling how properties appear in the UI (units, formatting, min/max, step).

## The `[ServiceMeasuringPoint]` Attribute

How to declare a measuring point and how it differs from a regular property.

## Property vs. Measuring Point

When to use a property versus a measuring point -- key differences in behavior and intent.

## Persistence

Which properties are persisted across restarts and how to control persistence behavior.
