node_modules/
docs/.vitepress/cache/
docs/.vitepress/dist/
temp/

# Harness state is never tracked; repo-scoped slash commands under .claude/commands/ are, because
# they are repo behaviour rather than settings (decision 0113 — rationale in retro-0 D4).
.claude/*
!.claude/commands/

# Planning artifacts live in architecture/specs/ (decision 0011 rule 2). The rest of docs/ is
# tracked, including the process substrate.
docs/superpowers/
